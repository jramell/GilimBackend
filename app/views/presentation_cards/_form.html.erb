<%= form_for(@presentation_card) do |f| %>
  <% if @presentation_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presentation_card.errors.count, "error") %> prohibited this presentation_card from being saved:</h2>

      <ul>
      <% @presentation_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artist_id %><br>
    <%= f.text_field :artist_id %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.text_field :song_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
