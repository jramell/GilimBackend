<p id="notice"><%= notice %></p>

<h1>Listing Events</h1>

<table>
  <thead>
    <tr>
      <th>Artist</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Name</th>
      <th>Placename</th>
      <th>Description</th>
      <th>Assistants</th>
      <th>Date</th>
      <th>Followercount</th>
      <th>Converted image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.artist %></td>
        <td><%= event.latitude %></td>
        <td><%= event.longitude %></td>
        <td><%= event.name %></td>
        <td><%= event.placeName %></td>
        <td><%= event.description %></td>
        <td><%= event.assistants %></td>
        <td><%= event.date %></td>
        <td><%= event.followerCount %></td>
        <td><%= event.converted_image %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
